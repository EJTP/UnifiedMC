# Copy to env.sh and fill in. env.sh is gitignored.

# The server you play on.
export UNIFIEDMC_SERVER=mc.example.com:25565

# Port the server-side mod publishes on. Must match config/unifiedmc.properties there.
export UNIFIEDMC_MANIFEST_PORT=25566

# Azure application (client) id, for signing in with Microsoft.
# Without it the launcher falls back to an offline profile.
# export UNIFIEDMC_CLIENT_ID=

# CurseForge API key. Without it the mod catalogue is Modrinth only.
# export UNIFIEDMC_CF_KEY=
